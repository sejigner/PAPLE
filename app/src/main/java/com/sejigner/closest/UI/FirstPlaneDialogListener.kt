package com.sejigner.closest.UI

import com.sejigner.closest.room.FirstPaperPlanes

interface FirstPlaneDialogListener {
    // Create a function to add items
    // in FirstPaperPlanes on clicking
    fun onDiscardButtonClicked(item:FirstPaperPlanes)
}